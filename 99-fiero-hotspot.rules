# NOTE: @TARGET_USER@ is dynamically replaced by install.sh during installation.
ACTION=="change", SUBSYSTEM=="power_supply", ATTR{type}=="Mains", RUN+="/usr/bin/systemd-run --no-block --collect -- /usr/bin/su - @TARGET_USER@ -c /usr/local/bin/fiero-prompt"
# END OF FILE
